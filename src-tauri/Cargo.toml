[package]
name = "upload-test-file-generator"
version = "0.1.2"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0.2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
tauri = { version = "2.2.5", features = [] }
